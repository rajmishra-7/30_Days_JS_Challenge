// Array Iteration

let arr = [10, 20, 30, 40, 50];

// for loop
for (let index = 0; index < arr.length; index++) {
    const element = arr[index];
    console.log(element);
    
}

// for-each loop
arr.forEach(element => {
    console.log(element);
    
});